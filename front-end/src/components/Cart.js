import react from "react";

function Cart(props) {
  console.log(props);
  //   const result = props.obj.name && <p>{props.obj.name}</p>;
  return (
    <div>
      <p>Shopping Cart</p>
    </div>
  );
}

export default Cart;
